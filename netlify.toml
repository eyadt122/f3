[build]
  base      = "ia-project-main"
  command   = "npm run build"
  publish   = "build"

[build.environment]
  NODE_VERSION = "20"

# Serve index.html for all routes (single-page app)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
